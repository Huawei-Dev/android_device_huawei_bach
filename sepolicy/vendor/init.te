allow init proc_appinfo:file r_file_perms;
allow init fingerprintd:binder { transfer call };
allow init hal_lineage_touch_hwservice:hwservice_manager { add };
allow init hidl_base_hwservice:hwservice_manager { add };

allow init persist_file:dir { mounton };
allow init cust_file:dir { mounton };
allow init log_file:dir { mounton };
allow init produce_file:dir { mounton };
allow init version_file:dir { mounton };

allow init sysfs:file { setattr };

allow init wcnss_device:chr_file write;

# Allow init to bind mount loader config for media swcodec
allow init system_file:file mounton;

# adsprpcd
allow init adsprpcd_file:filesystem { mount relabelfrom relabelto };

# LEDs
allow init sysfs_leds:lnk_file read;

allow init sysfs:file setattr;

